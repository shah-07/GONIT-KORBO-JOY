#include <stdio.h>

int main()
{
    int a = 10;
    int b = 0;

    // Multiplying by 0
    printf("%d x %d = %d", a, b, a * b);

    // Dividing by 0
    printf("%d / %d = %d", a, b, a / b);

    return 0;
}
